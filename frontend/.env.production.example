# Production build — baked into npm run build (Create React App).
# Same-origin nginx proxy (docker compose):
REACT_APP_API_URL=/api
#
# Or absolute public API URL:
# REACT_APP_API_URL=https://your-domain.by/api

# Google OAuth (OAuth client id)
REACT_APP_GOOGLE_CLIENT_ID=
